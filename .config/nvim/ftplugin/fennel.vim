setlocal tabstop=2 " number of spaces that a tab counts for
setlocal shiftwidth=2 " number of spaces to use for each step of autoindent
setlocal softtabstop=2 " number of spaces to delete when pressing <BS> or other editing operations
setlocal commentstring=;%s
" let b:autopairs_enabled=0
